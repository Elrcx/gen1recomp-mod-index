# Rotate Down After Heal

Turns the player down after Pokemon Center healing sequence is complete (after the final dialog closes), to prevent starting dialogue again by accident.

Enable `rotate_down_after_heal` in the F10 mod manager or in `options.lua`.
